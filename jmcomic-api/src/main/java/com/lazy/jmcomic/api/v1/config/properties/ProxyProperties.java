package com.lazy.jmcomic.api.v1.config.properties;

import com.lazy.jmcomic.api.v1.constant.Properties;

import org.springframework.boot.context.properties.ConfigurationProperties;
import org.springframework.boot.context.properties.bind.DefaultValue;
//迁移至DevProperties
/*
@ConfigurationProperties(Properties.CONNECTION)
public record ProxyProperties(@DefaultValue("false") boolean enabled,
                              @DefaultValue("127.0.0.1") String host,
                              @DefaultValue("2080") int port) {
}
*/
